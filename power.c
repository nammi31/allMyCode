
   #include<stdio.h>
   #include<math.h>
   int main()
   {
       int x,a;
       long int r;
       scanf("%d %d",&x,&a);
       r=pow(x,a);
       printf("result=%d",r);
       return 0;
   }
