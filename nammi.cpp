#include<iostream>
#include<vector>
#include<map>
#include<queue>
#include<stack>
using namespace std;

struct nammi
{
    int a,b;
    char lol;
    double h;
}name;

#define pii pair<int,int>

int main()
{


    /*
    name.a=9;
    name.h=45.90;
    name.lol='m';
    cout<<name.a<<" "<<name.h<<" "<<name.lol<<endl;

    pii p;
    vector< pii >v;
    p.first=567;
    p.second=4567;
    v.push_back(p);
    p.first=776;
    p.second=0;
    v.push_back(p);
    v.push_back(pii(67,90));
    for(int i=0;i<v.size();i++)
    {
        cout<<v[i].first<<" "<<v[i].second<<endl;
    }
    /*
    vector<int>v(10,797);
    //for(int i=0;i<v.size();i++)
    {
        v.push_back(300);
        v.push_back(411);
        v.push_back(83);
        v.push_back(0);
    }
    for(int i=0;i<v.size();i++)
    {
        cout<<v[i]<<endl;
    }
    cout<<"first element="<<v[0]<<" "<<v.front()<<endl;
    cout<<"last element="<<v[v.size()-1]<<" "<<v.back()<<endl;
    v.pop_back();
    v.pop_back();
    cout<<"last element="<<v[v.size()-1]<<" "<<v.back()<<endl;


    stack<int>s;
    s.push(33);
    s.push(45);
    s.push(555);
    cout<<s.top()<<endl;
    if(s.empty()) cout<<"empty"<<endl;
    else cout<<"not empty"<<endl;
    s.pop();
    s.pop();
    cout<<s.top()<<endl;
    cout<<s.size()<<endl;



    queue<int>q;
    q.push(33);
    q.push(45);
    q.push(555);
    cout<<q.front()<<endl;
    if(q.empty()) cout<<"empty"<<endl;
    else cout<<"not empty"<<endl;
    q.pop();
    q.pop();
    cout<<q.front()<<endl;
    cout<<q.size()<<endl;

    */



    return 0;
}
